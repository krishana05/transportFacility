export interface Ride {
  id: number;
  empId: string;
  vehicleType: string;
  vehicleNumber: string;
  vacantSeats: number;
  time: string;
  pickupPoint: string;
  destination: string;
}
